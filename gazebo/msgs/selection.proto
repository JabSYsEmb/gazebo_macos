syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Selection
/// \brief A message for GUI selection data


message Selection
{
   uint32 id     = 1;
   string name   = 2;
   bool selected = 3;
}
