syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Publish
/// \brief Message that contains information about a publisher of data


message Publish
{
   string topic    = 1;
   string msg_type = 2;
   string host     = 3;
   uint32 port     = 4;
}
