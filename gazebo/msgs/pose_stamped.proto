syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface PoseStamped
/// \brief Message for a pose with a time


import "time.proto";
import "pose.proto";

message PoseStamped
{
   Time time          = 1; // Time when the data was captured
   Pose pose          = 2;
}


