syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Wrench
/// \brief Message for a wrench value


import "vector3d.proto";

message Wrench
{
   Vector3d force        = 1;
   Vector3d torque       = 2;
   Vector3d force_offset = 3;
}
