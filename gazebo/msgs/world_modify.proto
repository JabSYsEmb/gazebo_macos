syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface WorldModify
/// \brief A message that allows for modifying (open, close) worlds


message WorldModify
{
   string world_name = 1;
   bool remove = 2;
   bool create = 3;
   bool cloned = 4;
   string cloned_uri = 5;
}
