syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Wind
/// \brief A message containing a description of the global wind properties

import "vector3d.proto";

message Wind
{
   Vector3d linear_velocity = 1;
   bool enable_wind = 2;
}
