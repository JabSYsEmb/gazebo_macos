syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Battery
/// \brief Message for a battery

message Battery
{
  /// \brief Name of the battery
   string name    = 1;

  /// \brief Real voltage in volts.
   double voltage = 2;
}
