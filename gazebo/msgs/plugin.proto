syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Plugin
/// \brief A message containing visual information for gazebo::Plugin

message Plugin
{
   string name         = 1;
   string filename     = 2;
   string innerxml     = 3;
}
