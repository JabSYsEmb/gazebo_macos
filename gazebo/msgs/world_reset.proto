syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface WorldReset 
/// \brief A message that controls how the world is reset


message WorldReset
{
   bool all          = 1;
   bool time_only    = 2;
   bool model_only   = 3;
}


