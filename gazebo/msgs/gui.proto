syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface GUI 
/// \brief Message for a GUI

import "gui_camera.proto";
import "plugin.proto";

message GUI
{
   bool fullscreen  = 1;
   GUICamera camera = 2;
  repeated Plugin plugin    = 3;
}
