syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Param
/// \brief Param message.

import "any.proto";

message Param
{
   string name     = 1;
   Any    value    = 2;
  repeated Param  children = 3;
}
