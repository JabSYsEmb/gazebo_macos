syntax = "proto3";
package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface MeshGeom 
/// \brief Message for a mesh geometry


import "vector3d.proto";

message MeshGeom
{
   string filename = 1;
   Vector3d scale  = 2;
   string submesh  = 3;
   bool center_submesh = 4;
}
